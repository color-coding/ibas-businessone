package ${GroupId}.${ArtifactId}.${Domain.getName().toLowerCase()}.service.rest;

import jakarta.ws.rs.ext.Provider;

@Provider
public class JsonConfig extends org.colorcoding.ibas.bobas.jersey.JsonConfig {
}
